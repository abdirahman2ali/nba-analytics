# Postgres credentials have moved to .claude/.env
# Set these there:
#   NBA_DATABASE_URL=          Neon pooled connection string (application use)
#   NBA_DATABASE_URL_DIRECT=   Neon direct connection string (DDL/migrations only)
