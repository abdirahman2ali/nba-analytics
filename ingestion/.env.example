# Databricks credentials live in .claude/.env
# Set these there:
#   DATABRICKS_SERVER_HOSTNAME=  e.g. community.cloud.databricks.com
#   DATABRICKS_HTTP_PATH=        e.g. /sql/protocolv1/o/<org_id>/<cluster_id>
#   DATABRICKS_TOKEN=            personal access token
